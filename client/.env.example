# Optional when frontend is hosted separately from backend (e.g., Vercel / Netlify)
# If left empty or omitted, it defaults to '/api' for same-origin proxy or unified hosting.
VITE_API_URL=https://your-salary-maker-backend.onrender.com/api
